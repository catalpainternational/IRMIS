/**
 * Generate a popup on the map
 * @param {number} id
 * @param {string} featureType
 * @return [{ label: string, value: string }]
 */
export declare function GetDataForMapPopup(id: number, featureType: string): [{ label: string, value: string }];
